# 翻页采集：你的系统 vs 工具箱 —— 速度 / 翻页准确性 差距与优化

> 对比对象：逆向得到的「沃尔玛工具箱.exe」Tab1 ID采集 + 并发详情（证据：`沃尔玛/_re/dis/工具箱/沃尔玛工具箱.dis` 的 `_yh_worker`/`yahoo_page`/`_yh_detail_worker`）。
> 你的系统：`app/engine/collector.py`（`_paged_listing`/`_collect_details`）+ `proxy.py`（`pace`）。

---

## 一、差距根因（按影响排序）

| # | 维度 | 你现在 | 工具箱 | 影响 |
|---|---|---|---|---|
| **1** | **详情速度** | `_collect_details` **串行** + `pace()` 全局 **3–7s/请求** | **10 并发** worker + pxvid 池 + 自适应抖动 | 串行最慢；同样 100 个详情你要 ~8–12 分钟，它 ~1 分钟 |
| **2** | **翻页提前截断** | `if not fresh: break` —— **第一页全重复就停** | **连续 2 页无新增才停** | 沃尔玛会跨页重排/重复/夹赞助位，单个重复页就让你**少采后面所有页**（"不全"主因） |
| **3** | **发现广度受 25 页硬上限** | 只走沃尔玛 `/search`，`SEARCH_PAGE_CAP=25`（~1250 件/词），要靠人工拆价格区间 | **Yahoo `site:walmart.com/ip/ <kw>`** 独立索引通道 | 长尾商品沃尔玛搜索翻不到/被排名埋掉，它能挖到 |
| **4** | **失败率失控** | 封控才换 IP，无失败率感知 | 滑动窗口三级限流（10/7 降速、20/18 急停） | IP 半死时你继续猛打 → 大量 `give_up`，看着像"采不全/不准" |

---

## 二、工具箱实现要点（反汇编还原）

- **ID 采集走 Yahoo**（`_yh_worker`，dis 11600+）：查询串 `site:walmart.com/ip/ <关键词>`，翻页 `search.yahoo.com/search?p=&b={offset}&pz=10`；从结果页 `RU=` 跳转参数解出 `/ip/<slug>/<数字ID>`（`yahoo_extract`）。停止：**连续 2 页无新增 / 取页失败 / 达配额上限**；跨关键词全局去重；checkpoint 每 500 条自动存盘可断点续采。
- **并发详情**（`_yh_fetch_details`→`ThreadPoolExecutor(detail_workers)`，实采 10）：每 worker 从 pxvid 队列取令牌、从 91http 隧道取（每请求换出口 IP）；成功判据 `Accept-CH: Downlink`；**自适应失败率限流**（`_yh_detail_worker`）。

---

## 三、已落地优化（`app/engine/collector.py`，全部带单测）

### 优化1：翻页不再提前截断（准确性，默认生效）
`_paged_listing` 改为 **连续 `EMPTY_PAGE_TOLERANCE`(=2) 页无新增才停**（而非第一个重复页就停），并用页面自报 `maxPage` 收紧上限避免空翻。
→ 单个重复/赞助页不再误判到底，后续页的新品不再被漏。测试：`tests/test_pagination_yahoo.py`（容忍单个重复页 / 连续 2 空停 / 取页失败标 truncated）。

### 优化2：并发详情 + 自适应限流（速度，opt-in）
`WalmartCollector(detail_workers=N)`：
- N=1（默认）→ 保持原串行 + `pace()` 行为**完全不变**（向后兼容）。
- N>1 → `_collect_details_concurrent`：N 个并发请求**共用粘性 IP**，不走全局 `pace` 串行间隔，改由每请求前随机抖动控制节奏；`_AdaptiveThrottle` 滑动窗口（近10次≥7失败→🐢降速；近20次≥18失败→自动停，提示换IP重试失败项）保护粘性 IP。
- 单 IP 上并发越高越快也越易被封，**建议 3–5**。
- `_get(pace=...)` / `collect_detail(pace=...)` 加了开关。测试：`_AdaptiveThrottle` 降速/急停/恢复。

### 优化3：Yahoo `site:` 发现通道（广度，新增方法）
- `collect_ids_via_yahoo(keyword, max_pages, target)` → Walmart ID 列表（绕开 25 页硬上限；连续 2 页无新增/达 target 停）。
- `collect_by_keyword_yahoo(keyword, ...)` → 发现 ID + 二次采详情（并发由 `detail_workers` 控制）。
- `extract_walmart_ids_from_yahoo(html)` 纯函数（`RU=` → `/ip/<slug>/<id>`，去重保序），可单测。
- **建议**：沃尔玛搜索通道 + Yahoo 通道**双源合并**最大化覆盖（沃尔玛搜索拿排名靠前/有库存的，Yahoo 补长尾）。

---

## 四、实测验证（2026-06-07，真实 cliproxy 代理）

```
# Yahoo site: 发现（绕 25 页上限）
[Yahoo p1] 提到 7 个，新增 7  [Yahoo p2] 提到 7 个，新增 7  → 共 14 个真实 Walmart ID

# 并发4 采详情（同一粘性IP，复用不额外提IP）
并发4 采 4 个详情: 4 成功, 耗时 4.9s        # 串行需 ~16–36s（pace 3–7s/请求）→ 约 4–7x
  5022870636: ok Arctic Air $29.86 gtin13=0735541210235 upc=735541210235
  341451562 : ok MISSION    $35.99 gtin13=0818522029153 upc=818522029153
  9740219698: ok Better Homes $24.43 gtin13=0814645020091 upc=814645020091
  20292320104: ok JULEGIC   $18.80 gtin13=None upc=None     # marketplace 公开页无GTIN → 用改进3卖家后台补
```
顺带印证[[改进1]]：3/4 公开页直接拿到原生 gtin13+upc；第 4 个 marketplace 商品公开页没有 → 正是 `seller_gtin.backfill_gtin` 的活。

---

## 五、用法

```python
from app.engine.proxy import ProxyPool
from app.engine.collector import WalmartCollector
from app import config

pool = ProxyPool(api_key=config.PROXY_API_KEY, auto_rotate=True)
c = WalmartCollector(pool=pool, auto_rotate=True, detail_workers=4)   # 并发4

# A) 沃尔玛搜索通道（已修复提前截断）
r1 = c.collect_by_keyword("cooling fan", max_pages=25)
# B) Yahoo 通道（绕 25 页上限，挖长尾）
r2 = c.collect_by_keyword_yahoo("cooling fan", max_pages=20, target=300)
# 双源合并：去重 r1["listing"] + r2["ids"] 再统一采详情
```

> 注意：`detail_workers>1` 会更频繁地打粘性 IP，比原 3–7s 串行激进。生产按 IP 质量与封控容忍度调（建议 3–5 起步），失败率限流会自动降速/急停兜底。若要严格保守仍可设 `detail_workers=1`。

## 六、一句话总结
工具箱"翻页又快又全"靠三点，你都已补齐：① **并发详情**（不再串行+3–7s 死等）② **连续空页才停**（不再单个重复页就截断）③ **Yahoo `site:` 通道**（绕开沃尔玛 25 页硬上限挖长尾）。三者均已落地并真机验证。
